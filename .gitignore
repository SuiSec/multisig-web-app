# deps
node_modules

# env (never commit real values — see app/.env.example)
.env
.env.local
.env.*.local

# build output
dist
build
.output
.vite
*.tsbuildinfo

# generated per-build (produced by scripts/build-manifest.mjs, attached to releases)
dist-manifest.txt
dist-digest.txt
dist.zip

# os
.DS_Store
